$('.new_task').bind('ajax:success', function() { 
  $(this).parents('.project').find('.task_list').html("<%= escape_javascript(render 'tasks', project: @project) %>"); 
  $(this).parents('.project').find('.task_input_text, .task_input_date').val("");
});  
